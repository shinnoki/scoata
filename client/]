<div class="page-header">
  <h1>{{ item.name }}</h1>
  <a href="#" data-ng-click="remove()">delete</a></p>
</div>

<h3>Properties</h3>
<dl class="well">
  <p>
    <dt>Description:</dt>
    <dd>{{ item.description }}</dd>
  </p>
  <p>
    <dt>Min Value:</dt>
    <dd>{{ item.minValue }}</dd>
  </p>
  <p>
    <dt>Max Value:</dt>
    <dd>{{ item.maxValue }}</dd>
  </p>
  <p>
    <dt>Created at:</dt>
    <dd>{{ item.created_at | date:'yyyy年MM月dd日 HH:mm' }}</dd>
  </p>
  <p>
    <dt>Created by:</dt>
    <dd>{{ item.created_by.name }}</dd>
  </p>
</dl>

<h3>Your Record</h3>
<form class="form-horizontal" novalidate>
  <div class="control-group">
    <input class="input" type="number" ng-model="record.value" min ="{{ item.minValue }}" max="{{ item.maxValue }}" integer required>
    <button class="btn" ng-click="submit()">OK</button>
  </div>
</form>

<h3>Records</h3>
<table class="table table-hover table-condensed">
  <thead>
    <tr>
      <th>User</th>
      <th>Value</th>
      <th>Created At</th>
    </tr>
  </thead>
    <tr ng-repeat="record in records | orderBy:value:false">
      <td><a href="/user/{{ record.user._id }}"ng-cloak><img class="profile_image" ng-src="{{ record.user.profile_image_url }}"> {{ record.user.name }} </a></td>
      <td>{{ record.value }}</td>
      <td>{{ record.created_at | date:'yyyy年MM月dd日 HH:mm' }}</td>
    </tr>
</table>
